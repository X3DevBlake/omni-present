import React from 'react';
import HubWrapper from '@/components/hubs/HubWrapper';

export default function EncryptionProtocols() {
  return <HubWrapper name="Encryption Protocols" category="Network & Communication" />;
}